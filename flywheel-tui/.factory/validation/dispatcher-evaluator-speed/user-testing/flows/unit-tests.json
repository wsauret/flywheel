{
  "groupId": "unit-tests",
  "testedAt": "2026-03-24T03:13:00Z",
  "isolation": {
    "workingDirectory": "/Users/wsauret/Documents/GitHub/flywheel/flywheel-tui",
    "tool": "bun test",
    "noSharedState": true
  },
  "toolsUsed": ["bun test"],
  "assertions": {
    "VAL-DISP-001": {
      "status": "pass",
      "evidence": "Tests verify dispatcher uses configured engine (claude or opencode) via engineName parameter, not hardcoded. Both engines spawn correct binary.",
      "tests": [
        "SubprocessTransport: engine-aware command building > spawns 'claude' engine when engineName is 'claude'",
        "SubprocessTransport: engine-aware command building > spawns 'opencode' engine when engineName is 'opencode'",
        "Engine registry: dispatcher support > getEngine('claude') returns engine with buildDispatcherCommand",
        "Engine registry: dispatcher support > getEngine('opencode') returns engine with buildDispatcherCommand",
        "Engine registry: dispatcher support > Claude engine from registry builds correct dispatcher command",
        "Engine registry: dispatcher support > OpenCode engine from registry builds correct dispatcher command"
      ]
    },
    "VAL-DISP-002": {
      "status": "pass",
      "evidence": "Tests verify Claude Code dispatcher command includes --tools '', --model, --system-prompt, --no-session-persistence, --effort low, -p flag, --print. Tests also verify these flags are NOT present in worker buildCommand.",
      "tests": [
        "Claude engine: buildDispatcherCommand > builds dispatcher command with all optimization flags",
        "Claude engine: buildDispatcherCommand > disables all tools with --tools ''",
        "Claude engine: buildDispatcherCommand > passes system prompt via --system-prompt flag",
        "Claude engine: buildDispatcherCommand > passes prompt via -p flag (not stdin)",
        "Claude engine: buildDispatcherCommand > does NOT include --output-format or --input-format (not stream-json)",
        "Claude engine: buildDispatcherCommand > does NOT include --allowedTools (uses --tools '' instead)",
        "Claude engine: buildDispatcherCommand > does NOT include --resume flag",
        "Worker buildCommand is unchanged > Claude worker command does NOT contain dispatcher flags",
        "Worker buildCommand is unchanged > Claude worker with model does not get dispatcher defaults",
        "SubprocessTransport: engine-aware command building > Claude route includes --system-prompt flag (not concatenated)",
        "SubprocessTransport: engine-aware command building > Claude route passes prompt via -p flag (not stdin)"
      ]
    },
    "VAL-DISP-003": {
      "status": "pass",
      "evidence": "Tests verify OpenCode dispatcher command includes run, --format json, --model, and uses stdin for prompt delivery.",
      "tests": [
        "OpenCode engine: buildDispatcherCommand > builds dispatcher command with run and --format json",
        "OpenCode engine: buildDispatcherCommand > uses stdin for prompt delivery",
        "OpenCode engine: buildDispatcherCommand > does NOT include --session flag",
        "SubprocessTransport: engine-aware command building > OpenCode route passes prompt via stdin",
        "Worker buildCommand is unchanged > OpenCode worker command does NOT contain dispatcher-only flags"
      ]
    },
    "VAL-DISP-004": {
      "status": "pass",
      "evidence": "Tests verify dispatcher.model config value flows through as --model CLI flag for both engines. Default to Sonnet-class model when not configured.",
      "tests": [
        "SubprocessTransport: engine-aware command building > config.dispatcher.model flows through to --model CLI flag",
        "SubprocessTransport: engine-aware command building > config.dispatcher.model flows through to --model for opencode",
        "Config model flow through transport chain > dispatcher.model in config changes the --model flag (claude)",
        "Config model flow through transport chain > dispatcher.model in config changes the --model flag (opencode)",
        "Claude engine: buildDispatcherCommand > defaults to 'sonnet' model when no model provided",
        "Claude engine: buildDispatcherCommand > uses custom model when provided",
        "OpenCode engine: buildDispatcherCommand > defaults to 'anthropic/claude-sonnet-4-6' model when no model provided",
        "OpenCode engine: buildDispatcherCommand > uses custom model when provided"
      ]
    },
    "VAL-DISP-006": {
      "status": "pass",
      "evidence": "Tests verify auto-detect skips SDK for claude (always returns 'cli' label), and tries SDK first for opencode.",
      "tests": [
        "Auto-detect transport: engine-aware > skips SDK for claude engine — always uses CLI",
        "Auto-detect transport: engine-aware > tries SDK first for opencode engine (existing behavior)",
        "Auto-detect transport: engine-aware > passes engineName through to SubprocessTransport on fallback",
        "Auto-detect transport: engine-aware > passes dispatcherModel through to SubprocessTransport",
        "Auto-detect: never uses SDK for claude engine > never returns SDK label for claude engine",
        "Auto-detect: never uses SDK for claude engine > always uses CLI transport for claude engine regardless of SDK availability"
      ]
    },
    "VAL-DISP-007": {
      "status": "pass",
      "evidence": "Tests verify OpenCode NDJSON output is parsed line-by-line for text content, and Claude Code --print plain text output is parsed directly as JSON. Both extract valid decisions.",
      "tests": [
        "SubprocessTransport: engine-aware command building > OpenCode NDJSON output parsed correctly",
        "SubprocessTransport: engine-aware command building > Claude Code plain text output parsed correctly",
        "SubprocessTransport: engine-aware command building > Claude Code output with surrounding text is still parsed",
        "SubprocessTransport: engine-aware command building > response validates against DispatcherDecisionSchema (claude route)",
        "SubprocessTransport: engine-aware command building > response validates against DispatcherDecisionSchema (opencode route)",
        "extractTextFromNDJSON (shared utility) > extracts text from valid NDJSON text events",
        "extractTextFromNDJSON (shared utility) > returns empty string when no text events found",
        "extractTextFromNDJSON (shared utility) > skips non-JSON lines gracefully",
        "shared extractTextFromNDJSON used by both transports > dispatcher transport uses shared utility",
        "shared extractTextFromNDJSON used by both transports > evaluator transport uses shared utility"
      ]
    },
    "VAL-DISP-008": {
      "status": "pass",
      "evidence": "Tests verify that passing an unknown engine name throws a clear error containing the engine name.",
      "tests": [
        "SubprocessTransport: engine-aware command building > throws clear error when engine binary not found (claude)",
        "SubprocessEvaluatorTransport: engine-aware command building > throws clear error when engine binary not found (unknown engine)",
        "createEvaluatorTransport > throws when engine binary not found (nonexistent engine)",
        "Engine registry > throws on unknown engine"
      ]
    },
    "VAL-SDK-001": {
      "status": "pass",
      "evidence": "Tests verify SDK transport passes model parameter (providerID + modelID) in session.prompt() body. Default and custom model cases both covered.",
      "tests": [
        "SdkTransport: model parameter > passes model in session.prompt() body when dispatcherModel is provided",
        "SdkTransport: model parameter > passes model with default sonnet when no dispatcherModel specified",
        "SdkTransport: model parameter > parses compound model string (provider/model) into providerID and modelID",
        "SdkTransport: model parameter > handles model string without provider prefix by using 'anthropic' as default provider"
      ]
    },
    "VAL-SDK-003": {
      "status": "pass",
      "evidence": "Tests verify SdkTransport throws when engine is 'claude' or any non-opencode engine. Auto-detect never returns SDK for claude.",
      "tests": [
        "SdkTransport: OpenCode-only guard > throws clear error when engine is 'claude' (SDK is OpenCode-only)",
        "SdkTransport: OpenCode-only guard > throws clear error for any non-opencode engine",
        "SdkTransport: OpenCode-only guard > does NOT throw when engine is 'opencode'",
        "SdkTransport: OpenCode-only guard > does NOT throw when engine is not specified (defaults to opencode)",
        "Auto-detect: never uses SDK for claude engine > never returns SDK label for claude engine",
        "Auto-detect: never uses SDK for claude engine > always uses CLI transport for claude engine regardless of SDK availability",
        "Auto-detect transport: engine-aware > skips SDK for claude engine — always uses CLI"
      ]
    },
    "VAL-EVAL-001": {
      "status": "pass",
      "evidence": "Tests verify evaluator subprocess transport uses configured engine (claude or opencode), not hardcoded. Uses engine registry for command building.",
      "tests": [
        "SubprocessEvaluatorTransport: engine-aware command building > spawns 'claude' engine when engineName is 'claude'",
        "SubprocessEvaluatorTransport: engine-aware command building > spawns 'opencode' engine when engineName is 'opencode'",
        "SubprocessEvaluatorTransport: engine-aware command building > uses engine registry for command building (not hardcoded)",
        "createEvaluatorTransport > creates a transport with claude engine",
        "createEvaluatorTransport > creates a transport with opencode engine",
        "createEvaluatorTransport > created transport uses engine for command building"
      ]
    },
    "VAL-EVAL-002": {
      "status": "pass",
      "evidence": "Tests verify evaluator includes all optimization flags for Claude (--tools '', --model, --no-session-persistence, --effort low, --system-prompt, -p) and --model for OpenCode. Evaluator-specific prompt content verified.",
      "tests": [
        "SubprocessEvaluatorTransport: engine-aware command building > Claude route includes all dispatcher optimization flags (tools disabled, model, etc.)",
        "SubprocessEvaluatorTransport: engine-aware command building > Claude route uses --system-prompt for evaluator prompt (separate for caching)",
        "SubprocessEvaluatorTransport: engine-aware command building > Claude route passes evaluator input via -p flag (not stdin)",
        "SubprocessEvaluatorTransport: engine-aware command building > OpenCode route passes --model flag",
        "SubprocessEvaluatorTransport: engine-aware command building > OpenCode route passes evaluator input via stdin",
        "SubprocessEvaluatorTransport: engine-aware command building > evaluator prompt contains evaluator-specific content (not dispatcher content)",
        "createEvaluatorTransport > passes evaluatorModel through to the transport"
      ]
    },
    "VAL-CFG-001": {
      "status": "pass",
      "evidence": "Tests verify default dispatcher model is 'sonnet' for Claude and 'anthropic/claude-sonnet-4-6' for OpenCode. Both engine buildDispatcherCommand and SubprocessTransport default cases covered.",
      "tests": [
        "Default dispatcher models > Claude default dispatcher model is 'sonnet'",
        "Default dispatcher models > OpenCode default dispatcher model is 'anthropic/claude-sonnet-4-6'",
        "SubprocessTransport: engine-aware command building > defaults to 'sonnet' model for claude when not configured",
        "SubprocessTransport: engine-aware command building > defaults to 'anthropic/claude-sonnet-4-6' model for opencode when not configured",
        "SubprocessEvaluatorTransport: engine-aware command building > defaults to 'sonnet' model for claude when not configured",
        "SubprocessEvaluatorTransport: engine-aware command building > defaults to 'anthropic/claude-sonnet-4-6' model for opencode when not configured"
      ]
    },
    "VAL-CFG-002": {
      "status": "pass",
      "evidence": "Tests verify dispatcher.model in config changes --model flag for both engines. resolveModels() returns configured model, env var override confirmed via config loader tests.",
      "tests": [
        "Config model flow through transport chain > dispatcher.model in config changes the --model flag (claude)",
        "Config model flow through transport chain > dispatcher.model in config changes the --model flag (opencode)",
        "Config → evaluator transport model flow > resolveModels returns dispatcherModel that can be used as evaluatorModel",
        "Config → evaluator transport model flow > resolveModels falls back to global model",
        "SubprocessEvaluatorTransport: engine-aware command building > evaluatorModel flows through to --model CLI flag (claude)",
        "SubprocessEvaluatorTransport: engine-aware command building > evaluatorModel flows through to --model CLI flag (opencode)"
      ]
    },
    "VAL-CFG-003": {
      "status": "pass",
      "evidence": "Tests verify backward compatibility: no engineName defaults to legacy opencode, no dispatcherModel uses Sonnet defaults, resolveModels returns undefined when no model configured.",
      "tests": [
        "SubprocessTransport: engine-aware command building > backward compat: no engineName defaults to legacy opencode behavior",
        "SubprocessEvaluatorTransport: engine-aware command building > backward compat: no engineName defaults to legacy opencode behavior",
        "Config → evaluator transport model flow > resolveModels returns undefined when no model configured (engine default used)",
        "createEvaluatorTransport > defaults to opencode when no engine specified",
        "stage-loop-factory evaluator transport wiring > works without evaluatorTransport (backward compat)"
      ]
    },
    "VAL-CROSS-001": {
      "status": "pass",
      "evidence": "All 2963 tests pass (0 failures) across 118 files. Worker buildCommand tests explicitly verify dispatcher flags are NOT present in worker commands. SubprocessTransport does not export PhaseExecutor.",
      "tests": [
        "Worker buildCommand is unchanged > Claude worker command does NOT contain dispatcher flags",
        "Worker buildCommand is unchanged > OpenCode worker command does NOT contain dispatcher-only flags",
        "Worker buildCommand is unchanged > Claude worker with model does not get dispatcher defaults",
        "Worker buildCommand is unchanged > OpenCode worker with model does not get dispatcher defaults",
        "Worker spawn path unaffected > SubprocessTransport does not export or modify PhaseExecutor",
        "Full test suite: 2963 pass, 0 fail across 118 files"
      ]
    }
  },
  "frictions": [],
  "blockers": [],
  "summary": "Tested 15 assertions: all 15 passed. All 2963 unit tests pass (0 failures). Each assertion is covered by multiple dedicated test cases in the relevant test files. Worker spawn paths are explicitly verified unaffected by dispatcher/evaluator transport changes."
}
