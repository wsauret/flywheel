{
  "groupId": "retry-logic",
  "milestone": "evaluator-retry-and-revision",
  "testedAt": "2026-03-24T00:00:00.000Z",
  "isolation": {
    "repoRoot": "/Users/wsauret/Documents/GitHub/flywheel/flywheel-tui",
    "testFile": "tests/evaluator-retry.test.ts",
    "sourceFile": "src/evaluator/invoke.ts",
    "adapterFiles": ["src/tui/adapters/opentui.ts", "src/tui/adapters/headless.ts"]
  },
  "assertions": {
    "VAL-RETRY-001": {
      "status": "pass",
      "evidence": "Test 'transport called exactly once when evaluator returns passed:false' passes: callCount()===1, result.passed===false, result.cyclesUsed===1. Test 'returns immediately with reasoning from the failing result' passes. Source code (invoke.ts lines 161-170): valid passed:false hits early return with no retry — the for-loop breaks immediately on any valid EvaluatorResult.",
      "details": "2 tests pass. Source confirms: within the for-loop, a valid passed:false result returns immediately with feedback fields populated. No continue/retry path for valid results."
    },
    "VAL-RETRY-002": {
      "status": "pass",
      "evidence": "Tests pass: 'retries up to maxCycles on schema errors' (callCount===3 with maxCycles:3), 'succeeds if error retry eventually returns passing result' (2 errors then pass, callCount===3), 'returns passed:false with error message when all retries are errors'. Source code: catch block for non-timeout errors continues the for-loop (retry), timeout errors return immediately.",
      "details": "3 tests pass. Source confirms: the catch block in the for-loop continues to next cycle on non-timeout errors, retrying up to maxCycles. Timeout errors are handled separately with immediate return."
    },
    "VAL-RETRY-003": {
      "status": "pass",
      "evidence": "Tests pass: 'emits evaluator:invoked once even with error retries' (3 retries, invokedEvents.length===1), 'emits evaluator:invoked once when passed:false', 'emits evaluator:invoked once when passed:true', 'evaluator:invoked comes before evaluator:completed'. Source code (invoke.ts line ~139): emitter.evaluatorInvoked() is called once before the for-loop.",
      "details": "4 tests pass. Source confirms: evaluator:invoked is emitted at line 139, before the retry for-loop starts at line 143. The emission is outside and before the loop, guaranteeing exactly one emission."
    },
    "VAL-RETRY-005": {
      "status": "pass",
      "evidence": "Test 'returns immediately with passed:true and cyclesUsed:1' passes: result.passed===true, result.cyclesUsed===1, result.skipped===false, callCount()===1. Source code: passed:true path returns immediately inside the for-loop with cyclesUsed = cycle+1 = 1.",
      "details": "1 test passes. Source confirms: the passed:true branch (invoke.ts lines 152-158) returns immediately with cyclesUsed=1 on first successful invocation."
    },
    "VAL-RETRY-006": {
      "status": "pass",
      "evidence": "Tests pass: 'timeout returns passed:true, skipped:true with timed out reason' (result.passed===true, result.skipped===true, reason contains 'timed out'), 'timeout emits evaluator:failed event' (failedEvents.length===1, reason contains 'timeout'), 'timeout does not retry' (callCount()===1). Source code: isTimeoutError() check in catch block returns immediately with passed:true, skipped:true.",
      "details": "3 tests pass. Source confirms: TimeoutError is detected via isTimeoutError() (checks error.name === 'TimeoutError' or message includes 'timed out'), returns immediately with passed:true, skipped:true, emits evaluator:failed."
    },
    "VAL-RETRY-007": {
      "status": "pass",
      "evidence": "Tests pass: 'schema error on attempt 1 retries, then valid passed:false on attempt 2 returns immediately' (callCount===2, result.passed===false, cyclesUsed===2), 'valid passed:false does not consume retry cycles (maxCycles:1)' (callCount===1, maxCycles:1), 'error retries up to maxCycles:2, then returns on valid passed:false' (callCount===2). Source code: the for-loop only continues on catch (errors); valid results always return immediately.",
      "details": "3 tests pass. Source confirms: maxCycles controls the for-loop iteration count. Valid results (passed:true or passed:false) break out immediately via return. Only caught errors (non-timeout) continue the loop."
    },
    "VAL-RETRY-008": {
      "status": "pass",
      "evidence": "Tests pass: 'handler called exactly once regardless of internal error retries' (handlerCallCount===1 with subscribeToType), 'does not emit evaluator:invoked when skip_evaluation is true' (invokedEvents.length===0). Source inspection: OpenTUI adapter (opentui.ts line 369) handles evaluator:invoked with pushSystemText('checking output quality'). HeadlessAdapter (headless.ts line 179) logs 'Evaluator invoked for step'. Both are simple handlers called once per event — no accumulation or duplication logic.",
      "details": "2 tests pass. Code inspection confirms: both OpenTUI adapter (line 369-371) and HeadlessAdapter (line 179-182) have straightforward event handlers for evaluator:invoked that display a single message. Since the event is emitted exactly once (VAL-RETRY-003), the adapter is called exactly once — no idempotency issues."
    }
  },
  "testsRun": [
    {
      "command": "bun test tests/evaluator-retry.test.ts",
      "exitCode": 0,
      "output": "21 pass, 0 fail, 51 expect() calls. Ran 21 tests across 1 file. [20.00ms]"
    }
  ],
  "frictions": [],
  "blockers": [],
  "toolsUsed": ["bun test"],
  "summary": "Tested 7 assertions: all 7 passed. 21 unit tests in evaluator-retry.test.ts all pass (0 failures). Source code inspection of invoke.ts confirms retry logic correctly distinguishes valid results (immediate return) from transport errors (retry up to maxCycles). evaluator:invoked emitted once before retry loop. Adapter handlers in opentui.ts and headless.ts correctly handle single emission."
}
