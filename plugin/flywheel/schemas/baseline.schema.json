{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "flywheel/schemas/baseline.schema.json",
  "title": "Baseline (frozen spec)",
  "description": "Immutable copy of spec.json taken at work-start. Adds baseline_frozen_at timestamp. Composes task-list.schema.json via allOf. Requires task-list to use unevaluatedProperties (not additionalProperties) so baseline's extra key is allowed.",
  "allOf": [
    { "$ref": "task-list.schema.json" },
    {
      "type": "object",
      "properties": {
        "baseline_frozen_at": {
          "type": "string",
          "format": "date-time"
        }
      }
    }
  ]
}
