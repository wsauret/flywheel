/**
 * Shared Services
 *
 * Stateful, lifecycle-managed services for the TUI.
 */

export { timerService, useTimer, formatDuration } from "./timer"
export type { TimerStatus, PauseReason } from "./timer"
